# Which receipt scanner implementation to use: "mock" | "tesseract"
SCANNER=mock

# Database. For local dev a file URL works out of the box.
# Prod (Turso): libsql://<db>-<org>.turso.io  + DATABASE_AUTH_TOKEN
DATABASE_URL=file:local.db
DATABASE_AUTH_TOKEN=
